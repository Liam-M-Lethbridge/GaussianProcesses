Repository for custom implementation of Gaussian processes to test my own understanding and to also produce figures for a project.

## To build dockerfile
docker build -t gaussian-processes .

## To run container
make run

## To run container as shell
make shell